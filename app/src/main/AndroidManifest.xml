<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.flavour.invoice">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
            android:name=".InvoiceApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher_new"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_new_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".feature.launch.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".feature.main.MainActivity"
                android:label="@string/title_activity_main">
        </activity>
        <activity android:name=".feature.business.BusinessActivity">
        </activity>
        <activity android:name=".feature.invoice.InvoiceActivity">
        </activity>
        <activity android:name=".feature.item.ItemActivity">
        </activity>
        <activity android:name=".feature.charge.ChargeActivity">
        </activity>
        <activity android:name=".feature.billing.BillingDetailActivity">
        </activity>
        <activity android:name=".feature.viewer.ViewerActivity">
        </activity>
    </application>

</manifest>